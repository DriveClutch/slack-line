import React, { Component, PropTypes } from 'react'
import ReactDOM from 'react-dom';
import classNames from 'classnames';
import Immutable from 'immutable';

export default class SlackFeed extends Component {
    constructor(props){
        super(props);

        this.state = {

        }
    }
}